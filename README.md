# Harkingbade

Minimal Astro homepage deployed as Cloudflare Worker static assets.

## Commands

```sh
npm install
npm run dev
npm run build
```

The public site currently contains only a plain homepage and a minimal 404 fallback. Production deployment remains intentionally separate from staging.
