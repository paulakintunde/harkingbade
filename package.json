{
  "name": "harkingbade",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "npm run types:worker && astro check && astro build && node scripts/verify-build.mjs",
    "preview": "wrangler dev",
    "check": "astro check && tsc --noEmit",
    "verify:build": "node scripts/verify-build.mjs",
    "types:worker": "wrangler types",
    "deploy:dry": "npm run deploy:dry:production",
    "deploy:dry:staging": "npm run build && wrangler deploy --env staging --dry-run",
    "deploy:dry:production": "npm run build && wrangler deploy --env production --dry-run",
    "deploy:staging": "npm run build && wrangler deploy --env staging",
    "deploy:production": "npm run build && wrangler deploy --env production"
  },
  "dependencies": {
    "@astrojs/sitemap": "3.7.3",
    "astro": "7.2.2"
  },
  "devDependencies": {
    "@astrojs/check": "0.9.10",
    "@cloudflare/workers-types": "5.20260816.1",
    "typescript": "5.9.3",
    "wrangler": "4.123.0"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
